<settings>
    <servers>
        <server>
            <id>${env.MAVEN_REPO_NAME}</id>
            <username>${env.REPO_ACCESS_ID}</username>
            <password>${env.REPO_ACCESS_PASS}</password>
        </server>

    </servers>

</settings>